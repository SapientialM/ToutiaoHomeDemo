# 仿今日头条首页 - PM 审查 Checklist

PM 评审时按此 checklist 走一遍，避免遗漏行业最佳实践。审查输出会引用这里的条目 ID（如「数据加载·首屏能否成功加载」），方便后续追踪。

## 一、数据加载
- [ ] 首屏数据能否成功加载（< 2s 出现首条卡片）
- [ ] 加载失败时是否有错误提示（不是空白也不是闪退）
- [ ] 加载中是否有 loading 状态（不是直接空白或骨架屏缺失）
- [ ] 加载是否流畅（不是卡顿几秒才显示）
- [ ] 网络异常时是否有友好提示（文案+重试按钮）
- [ ] 下拉刷新是否可用 + 是否有视觉反馈
- [ ] 上拉加载更多是否可用 + 是否有 loading 提示
- [ ] 翻到最后一页是否禁用上拉 + 提示"已经到底了"

## 二、卡片展示
- [ ] 卡片内容是否完整（标题、来源、评论数、时间四要素齐全）
- [ ] 图片是否能正常显示（不裂、不白底）
- [ ] 图片加载中是否有占位图（避免布局抖动）
- [ ] 图片加载失败是否有 fallback（不显示 X 图标）
- [ ] 卡片间距是否合理（4dp/8dp 一致）
- [ ] 字体大小、行高是否符合阅读习惯（标题 16-18sp、正文 14-16sp）
- [ ] 来源/时间戳格式是否一致（如「3 分钟前」全卡片统一）
- [ ] 卡片点击区域是否足够大（>= 48dp）
- [ ] 标题最多 2 行，超出是否 ellipsis

## 三、交互
- [ ] 卡片点击是否跳详情
- [ ] 长按是否有合适反馈（Toast / 弹出菜单）
- [ ] 滚动是否流畅（无明显卡顿，FPS >= 50）
- [ ] 顶部 Tab 切换是否平滑（无白屏闪烁）
- [ ] 返回键是否行为正确（不是直接退出 App）

## 四、状态管理
- [ ] loading / error / success / empty 四种状态是否齐全
- [ ] 状态切换是否平滑（不是硬切）
- [ ] 异常状态是否给用户出路（重试按钮）
- [ ] 加载更多时是否区分「分页中」「已到底」

## 五、视觉规范
- [ ] 颜色使用是否一致（红色 #FF5757 用于品牌强调，不滥用）
- [ ] 字号是否分级别（标题/副标题/正文/辅助 4 级）
- [ ] 是否有不必要的分割线（Material 3 强调 spacing 优先）
- [ ] 暗色模式适配（暗色下文字背景对比度 >= 4.5:1）
- [ ] icon 是否统一风格（都是 Outlined 或都是 Filled）
- [ ] 留白/呼吸感是否充足（卡片内 padding >= 12dp）

## 六、架构与代码（如果能看到代码）
- [ ] 数据流是否单向、可追踪（StateFlow / Compose State）
- [ ] 状态管理是否集中（UiState 集中在一个 data class）
- [ ] 关键逻辑是否有注释（特别是数据转换、错误处理）
- [ ] Coroutine 作用域是否正确（viewModelScope / rememberCoroutineScope）
- [ ] 图片加载库是否统一（Coil 为主）

## 七、特殊加分项
- [ ] 是否实现 Room/SQLite 数据库存储（离线浏览）
- [ ] 是否支持离线浏览（无网能看历史）
- [ ] 是否有预加载（prefetch）下一个分页
- [ ] 是否有图片缓存策略（内存 + 磁盘）
- [ ] 是否埋点关键交互（点击卡片、Tab 切换、刷新）
