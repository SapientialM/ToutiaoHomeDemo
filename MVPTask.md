## 任务：今日头条风格 APP — 首页 MVP 完善与验收

**核心目标**：本次任务**只聚焦首页**，从**完整度、还原度、流畅性**三个维度全面对标 `./design` 目录下的真实截图，把首页及其所有子分页（关注、推荐、热榜、深圳、小说、发现、视频、财经）做到可交互、可演示的 MVP 级别。可以分模块逐步开发，每完成一个分页即可通过 PM 工具查看模拟器效果并沟通优化。

**设计参考**：所有界面截图已按对应分页名称保存在 `./design` 目录下，开发时请直接打开图片对照像素级还原。

---

### 一、全局与顶部优化

1. **顶部背景**：整个顶部区域（含手机状态栏）不要有任何灰色空白，必须铺满红色背景，对标截图中的红色头部。
2. **TopBar**：包含「关注、推荐、热榜、深圳」等分页按钮的横向滚动栏，按钮之间的 `padding` 减小，每个按钮底部选中时的红色横线要缩短，长度比两个字符的文本宽度稍短一些。
3. **浏览记忆**：首页下所有子界面都必须实现「上次看到这里，以下为新内容」的逻辑——记录用户上次浏览到的位置，返回后在该位置插入提示条，并继续向下加载更多内容。

---

### 二、各分页详细需求

#### 1. 首页-推荐
- 布局顺序：**5 条置顶新闻**（排列紧凑，无图或左侧小图）→ **「资讯速递」卡片**（带标题和小图）→ **正常图文列表**。
- 图文列表需支持**至少四种布局动态混合**：左文右图、上文下图、无图且紧凑、上文下视频（视频占位即可，暂不需要真实播放能力）。
- 列表需支持**动态加载**（下滑无限加载），到底后循环使用 Mock 数据。

#### 2. 首页-热榜
- 列表还原度已经较高，需要优化：
    - 每一行都要有**浅色渐变红色背景**（不用很深，淡淡的即可）。
    - 行与行之间的 `padding` 减小，让列表更紧凑。
- 页面顶部的「头条热榜」「2026 高考」等卡片，实际上是**可点击切换的热榜子分类**，需要设计和实现点击后切换榜单内容的逻辑。

#### 3. 首页-深圳
- 顶部**天气板块**：背景色需要与天气状况匹配（如晴天、阴天、雨天有不同色调），「切换城市」按钮要做成透明/毛玻璃效果。
- 下方列表同样支持**多种图文布局混合**。
- 在**第一栏或第二栏**需要插入一个**视频轮播图**（横向滑动，多视频封面轮播）。

#### 4. 首页-关注
- 布局顺序：
    1. 先显示**已关注作者的图文新闻**（Mock 2~3 条即可）。
    2. 然后是「更多内容」「你可能感兴趣的人」区域——这是一个**横向轮播卡片**，每张卡片展示推荐账号：头像、名字、灰色领域标签、红色「关注」按钮。
    3. 再往下是**图文推送列表**，类似朋友圈，每行数据右上角要有「关注」按钮。

#### 5. 首页-发现
- 直接对标小红书：左右**双列瀑布流**，上图下文，可**无限下拉**加载。

#### 6. 首页-小说
- 布局顺序：
    1. **书架轮播**：横向滑动，展示「为你推荐」等书籍封面。
    2. **排行榜区域**：顶部有「排行榜」「更多」按钮，以及「推荐榜/完结榜/阅读榜/高分榜/热搜榜」等分类标签。
    3. **三行轮播的排行榜显示**：展示排名和书籍。
    4. 下方是**无限下滑的小说列表**，每行：左侧封面图，右侧上方标题，下方简介 + 阅读量 + 状态（如连载/完结）。

#### 7. 首页-视频
- 无限下拉的视频列表，标准布局：**上文标题 + 下方视频封面/播放器**。
- 视频需要支持**自动播放**（进入可视区域自动播放，离开暂停），点击后进入**视频详情页**播放。

#### 8. 首页-财经（新增）
- 顶部显示**三个指数**：上证指数、深证成指、创业板指（Mock 数据即可，红绿涨跌颜色要对）。
- 下方是**上文下图**的无限下滑列表，每条底部有小头像 + 头条号名称 + 发布时间。

---

### 三、详情页需求

所有分页中的内容项点击后都需要进入对应的详情页：

1. **新闻详情页（通用）**：
    - 从上往下：作者头像 + 标题 + 领域标签 + 关注按钮 → 图片/视频内容区 → 正文文本 → 分享按钮 → 评论列表（可下滑）。
2. **视频详情页**：
    - 视频在列表中自动播放，点击后进入全屏/详情页继续播放。
3. **小说详情页**：
    - 刚打开是**书籍介绍界面**（封面、简介、开始阅读按钮），然后**右滑进入翻页阅读界面**（模拟小说翻页效果）。

---

### 四、数据与 Mock 策略

1. 当前项目使用 **Mock 数据**。很多现有 Mock 链接是虚假的，后续我会提供一部分**真实的图片 URL、视频 URL、新闻链接**给你替换。
2. **数量不会很多**，如果用户下滑到底部数据用完了，直接**无限循环**复用已有数据即可。
3. **详情页内容解析**：对于新闻类详情页，只有外部链接 URL 时，需要：
    - 先用 `curl` 或请求拉取网页内容；
    - 用**已有 LLM 能力**对网页进行智能解析，提取正文、标题、图片等，按要求的 JSON 结构填充；
    - 如果解析失败或网页无法访问，让 LLM 根据新闻标题**自动编撰合理正文**，并自动保存进 Mock 数据源，下次同一链接直接复用，不再重复解析。

---

### 五、执行建议

- 你可以按「全局顶部 → 推荐 → 热榜 → 深圳 → 关注 → 发现 → 小说 → 视频 → 财经 → 详情页」的顺序逐步开发。
- 每完成一个分页，**主动使用 PM 工具查看模拟器效果**，确认还原度后再进行下一个。
- 不需要向我汇报技术方案，直接动手实现即可；遇到截图理解不清的地方，随时打开 `./design` 目录下的对应图片对照。